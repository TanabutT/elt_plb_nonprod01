# Python Virtual Environment
# The 'venv' folder (and the common '.venv' alternative) stores the isolated Python environment.
# These files are specific to your local machine and should not be committed to Git.
venv/
.venv/

# Environment Variables
# The '.env' file is used to store sensitive information like API keys or database passwords.
# It should never be committed to version control.
.env

# Compiled Python files
# Python creates these files automatically.
__pycache__/
*.pyc

# Editor/IDE-specific files (Optional)
# If using VS Code, this folder stores user-specific workspace settings that aren't necessary to share.
.vscode/

# Docker-related local artifacts (Optional)
# Files generated by 'docker-compose' that are not essential for the repository itself.
# Adjust as needed based on your specific Docker workflow.
*.log